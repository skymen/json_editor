export const config = {
  id: "on-tab-selected",
  listName: "On tab selected",
  displayText: "On tab selected",
  description: "Triggered when the user switches to a different tab.",
  highlight: false,
  isTrigger: true,
};

export const expose = true;

export default function () {
  return true;
}
