export const config = {
  id: "on-key-removed",
  listName: "On key removed",
  displayText: "On key removed",
  description: "Triggered after a key has been removed from an object or a dictionary.",
  highlight: false,
  isTrigger: true,
};

export const expose = true;

export default function () {
  return true;
}
