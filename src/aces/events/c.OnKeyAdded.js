export const config = {
  id: "on-key-added",
  listName: "On key added",
  displayText: "On key added",
  description: "Triggered after a key has been added to an object or a dictionary.",
  highlight: false,
  isTrigger: true,
};

export const expose = true;

export default function () {
  return true;
}
